# Secrets — never commit these, this repo is public
.env
.env.*
!.env.local.example

# Real business data — this repo is public; the tracker holds real
# prospect names/phone numbers, so the data file itself stays local-only.
# Code that reads/writes it is tracked; the file's contents are not.
Lead_Tracker_and_Dashboard.xlsx
**/backups/*.xlsx

# Python
__pycache__/
*.pyc
venv/

# Node / Next.js
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# macOS
.DS_Store
